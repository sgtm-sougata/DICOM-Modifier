version: '3'

services:
  web:
    image: sougatamaity/dicom-modification:latest
    container_name: dicom_modification
    ports:
      - "8082:8501"
    volumes: 
      - dicom_modification:/app
    restart: always 

volumes:
  dicom_modification:
